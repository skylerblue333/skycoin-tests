{
  "name": "@skycoin4444/http-contract-assertions",
  "version": "0.2.0",
  "description": "Dependency-free deterministic HTTP response contract assertions",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check": "npm run build && npm test && npm audit --omit=dev --audit-level=high"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
